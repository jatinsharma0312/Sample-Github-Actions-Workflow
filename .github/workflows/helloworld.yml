name: "Hello world workflow"

#Controls when the action will run. Triggers the workflow on push  sone to  master or production.

on: 
    push:
        branches:
            - main
            - production 

jobs:
    helloworld:
        runs-on: ubuntu-latest
        steps:
            - name: "Print hello world"
               run: echo "hello world"


               